services:
  web:
    build: .
    container_name: lunch_roulette_web
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      FLASK_APP: run.py
    volumes:
      - .:/app
    command: python run.py
    networks:
      - lunch_roulette_network

networks:
  lunch_roulette_network:
    driver: bridge
